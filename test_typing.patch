--- src/components/Chat/ChatView.tsx
+++ src/components/Chat/ChatView.tsx
@@ -212,6 +212,9 @@
     });
     
     setSocket(newSocket);
+    
+    // Mock typing user for testing UI
+    setTimeout(() => setTypingUsers(['Admin User']), 2000);
 
     newSocket.on('typing', (data: { userId: number, userName: string }) => {
       setTypingUsers((prev) => {
